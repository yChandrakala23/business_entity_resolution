# Core / Person 4 (pipeline, I/O)
pandas>=2.0
numpy>=1.24

# Person 1 (blocking / candidate generation)
rapidfuzz>=3.0
Unidecode>=1.3

# Person 3 (matching model)
scikit-learn>=1.3
lightgbm>=4.0
joblib>=1.3

# Optional ensemble members (matcher.py already guards these with
# try/except ImportError -- safe to omit, but include if Person 3 is
# actually using them for the ensemble)
# xgboost>=2.0
# catboost>=1.2